@model IEnumerable<LidavBankProj.Models.TransactionModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Source Account Holder
        </th>
        <th>
            Source Account ID
        </th>
        <th>
            Destenation Account Holder
        </th>
        <th>
            Destenation Account ID
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Time)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SrcAccount.ApplicationUser.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SrcAccount.AccountId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DstAccount.ApplicationUser.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DstAccount.AccountId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Time)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TransactionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TransactionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TransactionId })
        </td>
    </tr>
}

</table>
